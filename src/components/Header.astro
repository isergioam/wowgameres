---
import { Image } from "astro:assets";
import logoWhite from "../assets/images/WOWGamerES_Logo_white.webp";
import logoBlack from "../assets/images/WOWGamerES_Logo_black.webp";

const pathname = Astro.url.pathname;
const isLabPage = pathname.includes('/simulador') || pathname.includes('/laboratorio');
---
<header class="main-header">
    <div class="header-top-row">
        <section class="logo">
            <a href="/">
                <Image src={logoWhite} id="logo-white" alt="WOWGamerES Logo" width={180} height={45} format="avif" loading="eager" class="logo-img logo-white" />
                <Image src={logoBlack} id="logo-black" alt="WOWGamerES Logo" width={180} height={45} format="avif" loading="eager" class="logo-img logo-black" />
            </a>
        </section>

        <div class="search-container">
            <input type="text" id="quickSearch" placeholder="Buscar guía (ej: Joyería TBC)..." autocomplete="off">
            <ul id="searchResults" class="search-results" style="display: none;"></ul>
        </div>
        
        <nav class="main-nav">
            <ul>
                <li>
                  <a href="/" data-i18n="nav_home" aria-label="Inicio" title="Inicio" class="hearthstone-link">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="minimal-icon hearthstone-icon">
                      <path d="M3 11L12 3L21 11"/>
                      <path d="M5 10v10h14V10"/>
                      <path d="M10 22v-5a2 2 0 0 1 4 0v5"/>
                      <line x1="18" y1="4" x2="18" y2="8"/>
                    </svg>
                  </a>
                </li>
                <li>
                  <a href="/noticias/" data-i18n="nav_news" aria-label="Noticias" title="Noticias" class="news-link">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="minimal-icon news-icon">
                      <path d="M4 22h16a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2H8a2 2 0 0 0-2 2v16a2 2 0 0 1-2 2Zm0 0a2 2 0 0 1-2-2v-9c0-1.1.9-2 2-2h2"/>
                      <path d="M18 14h-8"/><path d="M15 18h-5"/><path d="M10 6h8v4h-8V6Z"/>
                    </svg>
                  </a>
                </li>
                <li>
                  <a href="/simulador/" aria-label="Simulador de Oro e IA" title="Nexo Craft: Simulador de Oro & IA Nexo" class="lab-link">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="minimal-icon lab-icon">
                      <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>
                    </svg>
                  </a>
                </li>
                <li>
                  <a href="/donar/" aria-label="Donar y Apoyar" title="Apoyar la Web" class="donate-link">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="minimal-icon donate-icon">
                      <path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z"/>
                    </svg>
                  </a>
                </li>
                <li>
                  <button id="darkModeToggle" aria-label="Cambiar tema">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" class="theme-icon moon">
                      <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79Z"/>
                    </svg>
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" class="theme-icon sun">
                      <circle cx="12" cy="12" r="4"/>
                      <line x1="12" y1="2" x2="12" y2="5"/>
                      <line x1="12" y1="19" x2="12" y2="22"/>
                      <line x1="2" y1="12" x2="5" y2="12"/>
                      <line x1="19" y1="12" x2="22" y2="12"/>
                      <line x1="4.93" y1="4.93" x2="6.76" y2="6.76"/>
                      <line x1="17.24" y1="17.24" x2="19.07" y2="19.07"/>
                      <line x1="4.93" y1="19.07" x2="6.76" y2="17.24"/>
                      <line x1="17.24" y1="6.76" x2="19.07" y2="4.93"/>
                    </svg>
                  </button>
                </li>
            </ul>
        </nav>
    </div>
</header>

<!-- Banner flotante de aviso de la nueva herramienta (Solo fuera de la sección simulador) -->
{!isLabPage && (
<div class="nexo-tool-banner" id="nexo-tool-banner">
    <span class="nexo-badge">✨ NUEVA HERRAMIENTA</span>
    <span class="nexo-text">Prueba <strong>Nexo Craft</strong>: Simulador de Oro & Asistente IA</span>
    <a href="/simulador/" class="nexo-link-btn">Probar Ahora →</a>
</div>
)}

<style>
    .nexo-tool-banner {
        position: fixed;
        top: 102px;
        left: 50%;
        transform: translateX(-50%);
        z-index: 1999;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.8rem;
        background: linear-gradient(135deg, #2563eb, #7c3aed);
        color: #ffffff;
        padding: 0.4rem 1.4rem;
        border-radius: 9999px;
        font-size: 0.85rem;
        box-shadow: 0 8px 24px rgba(37, 99, 235, 0.4);
        width: fit-content;
        max-width: 90%;
        border: 1px solid rgba(255, 255, 255, 0.25);
        transition: top 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
    }

    .nexo-tool-banner.scrolled {
        top: 84px;
    }

    @media (max-width: 768px) {
        .nexo-tool-banner {
            top: 125px;
            font-size: 0.78rem;
            padding: 0.35rem 0.9rem;
            gap: 0.5rem;
        }
        .nexo-tool-banner.scrolled {
            top: 105px;
        }
        .nexo-text {
            display: none;
        }
    }

    .nexo-badge {
        background: rgba(255, 255, 255, 0.25);
        color: #ffffff;
        font-weight: 900;
        padding: 0.15rem 0.5rem;
        border-radius: 6px;
        font-size: 0.72rem;
        letter-spacing: 0.04em;
    }

    .nexo-text {
        font-weight: 500;
    }

    .nexo-link-btn {
        background: #ffffff;
        color: #2563eb;
        font-weight: 800;
        padding: 0.2rem 0.7rem;
        border-radius: 8px;
        font-size: 0.78rem;
        transition: transform 0.15s, background 0.15s;
        text-decoration: none;
    }

    .nexo-link-btn:hover {
        transform: scale(1.04);
        background: #f8fafc;
    }

    .logo-img {
        max-width: 100%;
        height: auto;
    }
    .logo-white { display: none; }
    .logo-black { display: block; }

    :global(.dark-mode) .logo-white { display: block; }
    :global(.dark-mode) .logo-black { display: none; }

    @media (max-width: 768px) {
        .logo-img { width: 250px; }
    }
</style>

<script>
  (function() {
    const toggle = document.getElementById('darkModeToggle');

    function applyTheme(isDark: boolean) {
      if (isDark) {
        document.documentElement.classList.add('dark-mode');
      } else {
        document.documentElement.classList.remove('dark-mode');
      }
    }

    // Inicialización
    applyTheme(document.documentElement.classList.contains('dark-mode'));

    // Scroll listener para redimensionar la cabecera flotante y el banner sincronizadamente
    const header = document.querySelector('.main-header');
    const banner = document.getElementById('nexo-tool-banner');

    if (header) {
      const handleScroll = () => {
        if (window.scrollY > 20) {
          header.classList.add('scrolled');
          if (banner) banner.classList.add('scrolled');
        } else {
          header.classList.remove('scrolled');
          if (banner) banner.classList.remove('scrolled');
        }
      };
      window.addEventListener('scroll', handleScroll);
      handleScroll(); // Check inicial
    }

    if (toggle) {
      toggle.addEventListener('click', (event) => {
        const isDark = !document.documentElement.classList.contains('dark-mode');
        
        if (!(document as any).startViewTransition) {
          localStorage.setItem('theme', isDark ? 'dark' : 'light');
          applyTheme(isDark);
          return;
        }

        const x = event.clientX;
        const y = event.clientY;
        const endRadius = Math.hypot(
          Math.max(x, window.innerWidth - x),
          Math.max(y, window.innerHeight - y)
        );

        const transition = (document as any).startViewTransition(() => {
          localStorage.setItem('theme', isDark ? 'dark' : 'light');
          applyTheme(isDark);
        });

        transition.ready.then(() => {
          const clipPath = [
            `circle(0px at ${x}px ${y}px)`,
            `circle(${endRadius}px at ${x}px ${y}px)`
          ];
          document.documentElement.animate(
            {
              clipPath: isDark ? clipPath : [...clipPath].reverse()
            },
            {
              duration: 400,
              easing: 'ease-in-out',
              pseudoElement: isDark ? '::view-transition-new(root)' : '::view-transition-old(root)'
            }
          );
        });
      });
    }

    // Escuchar cambios en otras pestañas
    window.addEventListener('storage', (e) => {
      if (e.key === 'theme') {
        applyTheme(e.newValue === 'dark');
      }
    });

    const searchInput = document.getElementById('quickSearch') as HTMLInputElement;
    const resultsList = document.getElementById('searchResults');

    const professions = [
      { id: 'alquimia', name: 'Alquimia' },
      { id: 'encantamiento', name: 'Encantamiento' },
      { id: 'herreria', name: 'Herrería' },
      { id: 'ingenieria', name: 'Ingeniería' },
      { id: 'inscripcion', name: 'Inscripción' },
      { id: 'joyeria', name: 'Joyería' },
      { id: 'peleteria', name: 'Peletería' },
      { id: 'sastreria', name: 'Sastrería' },
      { id: 'mineria', name: 'Minería' },
      { id: 'herboristeria', name: 'Herboristería' },
      { id: 'desuello', name: 'Desuello' },
      { id: 'cocina', name: 'Cocina' },
      { id: 'pesca', name: 'Pesca' },
      { id: 'primeros-auxilios', name: 'Primeros Auxilios' }
    ];

    const expansions = [
      { id: 'midnight', name: 'Midnight (12.0)' },
      { id: 'tww', name: 'The War Within' },
      { id: 'df', name: 'Dragonflight' },
      { id: 'sl', name: 'Shadowlands' },
      { id: 'bfa', name: 'Battle for Azeroth' },
      { id: 'legion', name: 'Legión' },
      { id: 'wod', name: 'Draenor' },
      { id: 'mop', name: 'Pandaria' },
      { id: 'cata', name: 'Cataclysm' },
      { id: 'wotlk', name: 'Wrath of the Lich King' },
      { id: 'tbc', name: 'The Burning Crusade' },
      { id: 'vanilla', name: 'Classic (Vanilla)' }
    ];

    function searchGuides(query: string) {
      if (!query || query.length < 2) return [];
      const q = query.toLowerCase().trim();
      
      let matchedProf = professions.filter(p => p.name.toLowerCase().includes(q) || q.includes(p.name.toLowerCase()));
      let matchedExp = expansions.filter(e => e.name.toLowerCase().includes(q) || e.id.includes(q));

      if (matchedProf.length === 0) matchedProf = professions;
      if (matchedExp.length === 0) matchedExp = expansions.slice(0, 3);

      const results = [];
      for (const p of matchedProf) {
        for (const e of matchedExp) {
          results.push({
            title: `Guía de ${p.name} - ${e.name}`,
            url: `/guias/${p.id}/${e.id}/`
          });
        }
      }
      return results.slice(0, 6);
    }

    if (searchInput && resultsList) {
      searchInput.addEventListener('input', (e) => {
        const val = (e.target as HTMLInputElement).value;
        const matches = searchGuides(val);

        if (matches.length > 0) {
          resultsList.innerHTML = matches.map(m => 
            `<li><a href="${m.url}">${m.title}</a></li>`
          ).join('');
          resultsList.style.display = 'block';
        } else {
          resultsList.style.display = 'none';
        }
      });

      document.addEventListener('click', (e) => {
        if (!searchInput.contains(e.target as Node) && !resultsList.contains(e.target as Node)) {
          resultsList.style.display = 'none';
        }
      });
    }
  })();
</script>
